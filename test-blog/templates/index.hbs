{{> header }}

<main>
  <ul id="postlist">
    {{#each posts as |post| ~}}
    <li>
      <a href="posts/{{ post.meta.slug }}.html">{{ post.meta.title }}</a>
      <aside>{{ date post.meta.date }}</aside>
    </li>
    {{/each~}}
  </ul>
</main>

{{> footer }}